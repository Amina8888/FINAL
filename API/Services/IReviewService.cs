// Назначение: Управление отзывами, пересчёт рейтинга

// Task<bool> CanLeaveReview(Guid consultationId, Guid userId);
// Task LeaveReview(Guid specialistId, AddReviewDto dto, Guid clientId);
// Task RecalculateRating(Guid specialistId);
